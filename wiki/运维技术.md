# 🐳 运维技术知识库

> 编译自 `典阅教育/Docker`(20篇) + `K8s`(10篇) + `Linux`(34篇) + `CICD`(6篇) + `工作文档`(80篇) = 150+ 篇实战笔记
> 时间跨度: 2023-2026

---

## 一、Docker 容器化 (20篇)

### 安装与配置
```bash
# CentOS 安装 Docker（阿里云镜像源）
sudo yum install -y yum-utils
sudo yum-config-manager --add-repo http://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo
sudo yum install -y docker-ce docker-ce-cli containerd.io
systemctl enable docker --now

# 配置加速 + 生产环境配置（cgroup systemd + overlay2 + 日志限制）
sudo tee /etc/docker/daemon.json <<-'EOF'
{
  "registry-mirrors": ["https://82m9ar63.mirror.aliyuncs.com"],
  "exec-opts": ["native.cgroupdriver=systemd"],
  "log-driver": "json-file",
  "log-opts": { "max-size": "100m" },
  "storage-driver": "overlay2"
}
EOF
```

### 核心操作速查
| 操作 | 命令 |
|------|------|
| 拉取镜像 | `docker pull nginx:1.20.1` |
| 启动容器 | `docker run -d --restart=always -p 88:80 --name mynginx nginx` |
| 进入容器 | `docker exec -it 容器id /bin/bash` |
| 挂载卷 | `-v /data/html:/usr/share/nginx/html:ro` |
| 复制文件 | `docker cp 容器id:/path /host/path` |
| 保存镜像 | `docker save imageID -o name.tar` |
| 加载镜像 | `docker load -i name.tar` |
| 提交修改 | `docker commit -a "author" -m "msg" 容器id 新镜像:tag` |
| 清理空间 | `docker system prune -a` |
| 大小查看 | `docker system df -v` |

### Docker Compose
- 安装：Windows 环境完整配置流程
- 管理工具：**Dockge** — 美观易用的 Docker Compose 管理面板
- 脚本合集：常用 docker-compose.yml 模板

### AI 模型部署实战
> 完整流程：VMware → Ubuntu 虚拟机 → 交叉编译 → ADB 推送到开发板

1. **环境准备**：VMware Pro 15 + Ubuntu 预编译镜像
2. **USB 驱动**：安装 Rockchip 驱动（DriverAssitant v5.0）
3. **交叉编译**：`/opt/rv1126_rv1109_sdk` 工具链 + qtcreator
4. **模型部署**：YOLOv5 C 语言推理 → `./build.sh` → ADB 推送到 `/mnt/userdata/`
5. **注意事项**：ADB 通讯端口窗口必须保持开启

### AnythingLLM + Chroma 部署
- AnythingLLM 本地化打包迁移（`docker save/load`）
- Chroma 向量数据库：`docker run -d -p 8000:8000 --name chroma chromadb/chroma`
- 自定义 Dockerfile 构建 AnythingLLM 镜像（需代理解决 GitHub 超时）

---

## 二、Kubernetes (10篇)

### 集群搭建
- **版本**: k8s 1.15
- **组件**: kubeadm、kubelet、kubectl
- **节点**: Master + Worker 架构
- **网络**: Flannel/Calico 插件
- **安装脚本**: `K8s_installnew.sh`（自动化部署脚本）

### 流量入口：Ingress
- Ingress Controller 部署
- 路由规则配置
- HTTPS 证书绑定
- 多服务流量分发

### GitLab 部署
- 在 K8s 上部署 GitLab
- Runner 配置
- CI/CD 流水线集成

---

## 三、Linux 系统管理 (34篇)

### 核心服务

| 服务 | 用途 | 部署方式 |
|------|------|---------|
| **Nginx** | Web 服务器 / 反向代理 / SSL | yum 安装 + 单独开启 SSL/HTTP2 模块 |
| **SRS** | 流媒体服务器（直播/推流） | CentOS 源码搭建 + Nginx 代理 HTTPS |
| **Confluence** | 团队知识库 | 完整安装配置流程 |
| **Jupyter Hub/Lab** | 数据科学环境 | Ubuntu 安装配置 |
| **OpenStack** | 私有云平台 | 二进制安装（528 行详细文档）|
| **OpsAny** | 运维自动化平台 | 部署配置 |
| **1Panel** | Linux 服务器管理面板 | 一键安装 |

### SSL 证书方案

| 方案 | 适用场景 |
|------|---------|
| **Let's Encrypt** | 公网域名，免费自动续期 |
| **mkcert** | 内网/本机开发环境，自签可信证书 |
| **acme.sh** | 自动化证书申请与续期 |
| **自建 CA** | 企业内网 SSL |

### 常用操作
- CentOS 7 Nginx 彻底卸载重装
- CentOS 系统 SSL 证书生成
- Nginx 反向代理配置
- Linux 一键安装 Docker + Docker Compose
- 企业级 yum 源配置（1332 行详细文档）
- 离线安装包制作
- 编译安装 OpenSSL
- RedHat/CentOS root 密码破解
- 二进制安装 glibc 依赖库

### 运维自动化
- **SRS + Nginx 开机自启**：流媒体服务自动恢复
- **企业级 yum 源**：内网包管理
- **CDN 加速配置**
- 局域网可信任 SSL 证书搭建

---

## 四、CI/CD 持续集成 (6篇)

### Jenkins + GitLab 自动化打包流水线

| 项目类型 | 技术栈 | 目标环境 | 文档篇幅 |
|---------|--------|---------|---------|
| .NET 后端 | .NET Core | → K8s | 318 行 |
| Java 后端 | Spring Boot / Maven | → K8s | 326 行 |
| PHP 项目 | PHP + Composer | → K8s | 309 行 |
| 前端项目 | Node.js / npm / build | → K8s | 527 行 |
| Android App | Gradle / SDK | → APK | 513 行 |

### 统一流水线架构
```
Git Push → GitLab Webhook → Jenkins Trigger
  → Pull Code → Build/Compile → Docker Build
  → Push Image → Deploy to K8s
```

---

## 五、OpenStack 与虚拟化平台

### OpenStack 部署

| 部署方式 | 说明 | 适用场景 |
|---------|------|---------|
| **DevStack** | 一键安装脚本，快速搭建开发环境 | 学习测试 |
| **Fuel** | Mirantis 可视化部署工具 | 生产环境 |
| **二进制安装** | 手动逐步安装各组件 | 深入了解架构 |

**踩坑记录**：
- Keystone 安装时注意端点配置，`service_project_id` 和 `role_id` 需准确关联
- Fuel Master 节点在 VirtualBox 中需配置双网卡（外网 + 管理网）
- OpenStack 单机安装脚本适合 PoC，生产环境建议多节点 Fuel 部署

### Proxmox+Ceph 超融合

Proxmox VE（PVE）是基于 Debian 的企业级虚拟化平台，集成 Ceph 分布式存储实现 HCI：

| 组件 | 作用 |
|------|------|
| **Proxmox VE** | KVM 容器虚拟化 + Web 管理 |
| **Ceph** | 分布式存储（OSD/MON/MGR） |
| **物理网络** | 业务网 + 存储网分离，万兆互联 |

### KVM 虚拟化

- **CentOS 安装 KVM**：`yum install qemu-kvm libvirt virt-install`
- **virt-viewer**：SPICE/VNC 客户端连接虚拟机
- **磁盘格式**：raw（性能优先）vs qcow2（快照优先）

### VMware 虚拟化

- **vSphere**：ESXi + vCenter 集群管理
- **VMware 配置共享物理机 IP**：桥接模式 + 静态路由
- **黑群晖 NAS**：虚拟机部署 Synology DSM 系统，用于文件共享和备份

### 其他虚拟化

| 方案 | 用途 |
|------|------|
| **VMware 安装 OpenWRT** | 软路由，旁路由模式实现科学上网 |
| **Nano 轻量虚拟机平台** | 资源有限的轻量 KVM 管理 |
| **Portainer** | Docker 可视化管理 |

---

## 六、监控与告警

### Zabbix（企业级监控）

CentOS 安装 Zabbix 的典型架构：

```
Zabbix Server → 数据库 (MySQL/PostgreSQL) → Web 前端
     ↓
Zabbix Agent (被监控主机)
```

**配置要点**：Server/Agent 的 `Server` 和 `ServerActive` 参数指向正确 IP，防火墙放行 10050/10051 端口。

### Prometheus + Grafana

Prometheus 云原生监控栈，配合 Grafana 可视化：

- **Pull 模型**：主动抓取指标数据
- **Exporters**：Node Exporter（主机）、Blackbox Exporter（端口探活）
- **告警**：AlertManager 处理告警路由

### TenSunS — Consul 驱动的监控自动发现平台

**TenSunS**（原 vops）是基于 Consul 的 Prometheus 监控增强工具，解决大规模监控中**目标自动发现**的痛点：

| 组件 | 作用 |
|------|------|
| **Consul** | 服务注册中心，存储所有监控目标元数据 |
| **TenSunS Web** | 可视化页面管理 Consul 中的监控配置 |
| **Blackbox Exporter** | 站点拨测（TCP/ICMP/HTTPS），经 Consul 自动发现配置 |
| **Prometheus** | 从 Consul 自动拉取监控目标列表 |

**核心流程**：在 TenSunS Web 页面上配置监控目标 → 写入 Consul → Prometheus 通过 Consul SD 自动发现 → 开始采集指标。

**Blackbox 站点监控**：通过 TenSunS 页面批量导入监控目标，自动关联 Blackbox Exporter 配置，支持 TCP/ICMP/HTTPS 拨测。内置 Grafana Dashboard ID `9965`，按公司/项目/环境分组展示。

**Consul 部署**（CentOS 7）：
```bash
yum install -y yum-utils
yum-config-manager --add-repo https://rpm.releases.hashicorp.com/RHEL/hashicorp.repo
yum -y install consul
```

> TenSunS 适合管理数百个监控目标的场景，通过 Consul 的标签体系（company/project/env）实现监控目标的多维度分类。

---

## 七、Windows 服务器运维

### 基础管理

| 场景 | 工具/方法 |
|------|----------|
| **远程桌面** | RDP + VPN，配置用户权限和登录审计 |
| **SVN 服务器** | VisualSVN Server，权限分组管理 |
| **FTP 文件服务** | FileZilla Server / IIS FTP，SSL 加密传输 |
| **Windows 服务管理** | WinSW 将任意 exe 安装为 Windows 服务 |

### Redis 多实例部署

```powershell
# 复制 Redis 目录，修改端口配置
# redis.windows-service.conf → port 6380
redis-server --service-install redis.windows-service.conf --service-name Redis6380
```

### 禅道项目管理

- 一键安装包（Windows 集成 Apache + MySQL + PHP）
- 或 Docker 部署：`docker run -d -p 8080:80 easysoft/zentao`

### Datax 数据同步

阿里巴巴离线数据同步工具：`python datax.py job.json`

---

## 八、DevOps 工具链

### 代码与协作

| 工具 | 用途 | 部署方式 |
|------|------|---------|
| **GitLab** | 代码仓库 + CI/CD | 物理机/Docker 部署 |
| **Jira** | 项目管理 + 问题追踪 | Docker/独立部署 |
| **Confluence** | 团队知识库 | Docker/独立部署 |
| **Jumpserver** | 堡垒机/审计 | Docker Compose |
| **Teleport** | 开源堡垒机 | 一键部署 |

### 数据与中间件

| 工具 | 用途 |
|------|------|
| **Kettle (PDI)** | ETL 数据抽取转换 |
| **Tapdata Cloud** | 实时数据同步（单节点免费） |
| **Chemex** | 资产管理系统（运维 CMDB） |
| **datax** | 离线数据同步 |

---

## 九、实战经验总结

### 故障排查
1. **Docker 拉取镜像失败**: 配置阿里云镜像加速器
2. **服务器 yum 源不可用**: 修改 `/etc/resolv.conf`（国内 114.114.114.114 / 国外 8.8.8.8）
3. **CentOS 7 网络配置**: 网卡重启 `service network restart`
4. **ADB 设备未识别**: 检查 USB 是否被虚拟机接管

### 最佳实践
1. Docker 生产配置：cgroup driver = systemd + 日志 100m 上限
2. 镜像迁移：`docker save` → 内网传输 → `docker load`
3. K8s 安装：指定版本 `docker-ce-20.10.7` 以避免兼容问题
4. CI/CD 多语言支持：统一 Jenkins pipeline 模板

### 关键工具清单
| 工具 | 用途 | 安装方式 |
|------|------|---------|
| Docker | 容器引擎 | yum / 一键脚本 |
| Docker Compose | 多容器编排 | Windows 独立安装 |
| Dockge | Compose 管理面板 | Docker 部署 |
| K8s (kubeadm) | 容器编排 | 自动化脚本 / 二进制安装 |
| Jenkins | CI/CD 引擎 | Docker / 直接部署 |
| GitLab | 代码仓库 + CI | 物理机 / K8s / Docker |
| SRS | 流媒体服务器 | 源码编译 |
| 1Panel | 服务器管理面板 | 一键安装 |
| OpsAny | 运维自动化 | 部署配置 |
| **OpenStack** | 私有云平台 | DevStack / Fuel / 二进制 |
| **Proxmox VE** | 虚拟化平台 | ISO 安装 / Ceph 集成 |
| **Zabbix** | 企业级监控 | yum 安装 + 数据库 |
| **Prometheus** | 云原生监控 | Docker 部署 + Exporters |
| **VMware vSphere** | 企业虚拟化 | ESXi + vCenter |
| **Jumpserver** | 堡垒机/审计 | Docker Compose |
| **Teleport** | 开源堡垒机 | 一键部署 |
| **Jira** | 项目管理 | Docker |
| **Confluence** | 知识库 | Docker |
| **Kettle (PDI)** | ETL 数据同步 | 解压即用 |
| **Tapdata** | 实时数据同步 | 单节点部署 |