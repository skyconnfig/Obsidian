---
title: "白嫖 OpenRouter 免费模型 · 一分钱不花用顶尖大模型"
source: "https://echobird.ai/openrouter/"
author:
published:
created: 2026-05-30
description: "教你在 EchoBird 里用 OpenRouter 的免费模型 —— 每天 50 次免费请求，覆盖全球顶尖大模型。"
tags:
  - "clippings"
---
免费教程 · FREE GUIDE

## 白嫖 OpenRouter 免费模型

众所周知， [OpenRouter](https://openrouter.ai/) 是 AI 模型领域的大善人 —— 每天提供 **大量的免费额度** 。

新款大模型的全球首发几乎都在 OpenRouter，例如 DeepSeek、Qwen 等新版本，第一时间就能尝鲜。

于是就出现了这样一种情况:**你完全可以一分钱不花，免费用上最新最好用的大模型** 。 唯一的限制是:免费账号每天 *50 次请求* —— 安装工具、改改文档，相当够用了！ 如果你升级到 **Pay-as-you-go** 计划(也就是最少充值 $10)，所有免费模型的额度会变成 *每天 1000 次请求* 。

50免费账号 / 每日请求

1000充值 $10 后 / 每日请求

#1API Router 全球口碑

打开 [openrouter.ai](https://openrouter.ai/) 注册账号， 登录后点击首页 **Get API Key** 创建一个新的 API Key 并复制下来。

然后在顶部搜索框输入 `free` ，会列出所有当前免费可用的模型。挑一个你想用的(比如最新的 `deepseek` / `qwen` / `llama` 系列)， 把它的 **模型 ID** 也复制下来 —— 一般长这样:`deepseek/deepseek-v4-flash:free` 。

**注意:**OpenRouter 的免费模型 ID 在结尾会有 `:free` 后缀, **粘贴时要带上这个后缀** —— 去掉就会变成付费版。

![OpenRouter 注册并搜索 free 模型](https://echobird.ai/openrouter/1.png)

图 1 · OpenRouter 官网搜索 "free" 并申请 API Key

打开 EchoBird，左侧进入 **模型中心** ，在右侧栏切换到 **模型中转站** 标签 —— 第一个就是 **OpenRouter** 。点击它旁边的 `+` 按钮。

在弹出的「编辑模型配置」窗口里:

① **名称** 就是个标签，你能认出来就行，随便起。  
② **模型 ID** 才是真正传给 API 的字段，粘贴刚才复制的那个，例如 `deepseek/deepseek-v4-flash:free` ， **结尾的 `:free` 要保留** —— 带 `:free` 才会走免费版，去掉会变成付费版。  
③ **OpenAI 地址** 已经自动填好，无需改动。  
④ **API 密钥** 粘贴你在 OpenRouter 申请的那个 Key。  
⑤ 点 **保存** 。

**提示:**同一个 API Key 可以反复添加不同的模型，每个模型独立配置一行， 随时在「应用管理」、「我的 AI 项目」、「安装与修复」里切换使用。

![模型中心 → 模型中转站 → OpenRouter](https://echobird.ai/openrouter/2.png)

图 2 · 模型中心 → 模型中转站 → OpenRouter → 点 +

![填写模型 ID 和 API Key](https://echobird.ai/openrouter/3.png)

图 3 · 填模型 ID 与 API Key，保留:free 后缀

切到左侧的 **安装与修复** 页面，在右下角的模型选择器里挑刚才添加的那个 OpenRouter 模型，随便聊一句、跑一个快速指令试试 —— **你会发现免费的速度也相当快** ， 完全可以拿来日常解决一些临时问题。

遇到 `429` / 配额用完的提示就是当天 50 次用满了，等明天自动重置; 或者升级 Pay-as-you-go 把额度推到 1000 次/天。

![在安装与修复界面测试 OpenRouter 模型](https://echobird.ai/openrouter/4.png)

图 4 · 在「安装与修复」里直接用刚配好的 OpenRouter 模型对话

FREE GUIDE

## Free LLMs on OpenRouter

[OpenRouter](https://openrouter.ai/) is, famously, the most generous host in the AI space — **a real, daily free tier** of model credits.

Most new frontier models debut globally on OpenRouter — DeepSeek, Qwen and others land their latest releases there first, ready to try on day one.

The result: **you can run the latest, top-tier LLMs without paying a cent**. The only cap is *50 requests/day* on a free account — plenty for installing tools or polishing docs. Top up the account into the **Pay-as-you-go** tier (a one-time $10 minimum) and every free model bumps up to *1,000 requests/day*.

50free account · req/day

1000after $10 top-up · req/day

#1API Router · global

Head to [openrouter.ai](https://openrouter.ai/), create an account, then click **Get API Key** on the homepage. Generate a key and copy it somewhere safe.

Type `free` in the search box at the top — every currently-free model will show up. Pick one you want to try (the newest `deepseek` / `qwen` / `llama` family is a good start) and copy its **model ID**. It looks like `deepseek/deepseek-v4-flash:free`.

**Heads up:** Free models on OpenRouter show a `:free` suffix — **keep it when you paste** (dropping it switches to the paid variant).

![Sign up on OpenRouter and search for free models](https://echobird.ai/openrouter/1en.png)

Fig 1 · Search "free" on OpenRouter and create an API Key

Open EchoBird, go to **Model Nexus** in the left sidebar, then switch the right-side pane to the **Model Relays** tab. **OpenRouter** is the first entry. Click the `+` button next to it.

In the "Edit Model Config" dialog:

① **Name** is just a label — type anything you'll recognise.  
② **Model ID** is the field that gets sent to the API — paste the ID you copied, e.g. `deepseek/deepseek-v4-flash:free`, and **keep the `:free` suffix** — it selects the free variant; dropping it bills you for the paid one.  
③ **OpenAI URL** is pre-filled — leave it alone.  
④ **API Key**: paste the OpenRouter key.  
⑤ Hit **Save**.

**Tip:** One key can back as many models as you want — add a separate row per model and switch freely from App Manager / My AI Projects / Install & Repair.

![Model Nexus → Model Relays → OpenRouter](https://echobird.ai/openrouter/2en.png)

Fig 2 · Model Nexus → Model Relays → OpenRouter → hit +

![Fill in the Model ID and API Key](https://echobird.ai/openrouter/3en.png)

Fig 3 · Model ID must keep the:free suffix

Jump to **Install & Repair** on the left, pick the OpenRouter model from the model selector at the bottom right, and send a quick message — **free latency is genuinely fast**. It is more than enough for everyday quick-fix prompts.

If you see a `429` / quota error, you have hit the 50-request daily cap — it resets next day. Or upgrade to Pay-as-you-go to bump it to 1,000/day.

![Test the OpenRouter model inside Install & Repair](https://echobird.ai/openrouter/4en.png)

Fig 4 · Chat directly with the OpenRouter model you just configured