# 美股抓取系统

```bash
好，我直接给你一套**可用于生成完整项目的“高质量提示词（Prompt）”**，你可以丢给任何大模型（包括我）去一键生成代码👇

我按**模块拆分**，你可以分步骤生成，也可以一次性全生成。

---

# 🚀 一、整站级 Prompt（推荐直接用这个）

```text
你是一名资深全栈架构师，请帮我从0到1生成一个完整的股票数据分析系统，要求如下：

【技术栈】
后端：
- FastAPI（Python 3.11+）
- SQLAlchemy / SQLModel
- MySQL（可扩展ClickHouse）
- Redis缓存
- 使用yfinance作为数据源

前端：
- Vue3 + Vite + TypeScript
- ant-design-vue
- Pinia
- Vue Router（history模式）
- ECharts用于K线图
- 左右布局后台管理系统风格

【UI主题】
const themeOverrides = {
  common:{
    primaryColor: '#111827',
    primaryColorHover:'#000000',
    textColorBase:'#1F2937',
    textColor2:'#6B7280',
    borderColor: '#E5E7EB'
  }
}

【核心功能】
1. 股票列表（支持分页、搜索）
2. 股票历史数据（支持10年K线）
3. K线图展示（蜡烛图 + 成交量）
4. 自选股功能（前端本地存储即可）
5. 数据缓存（Redis）
6. 后端定时任务自动抓取数据（每天更新）

【后端要求】
- 使用分层结构（router/service/model）
- 提供RESTful API：
  - /stocks/list
  - /stocks/history
  - /stocks/page
- 支持分页查询
- 使用Pydantic定义返回结构
- 加入缓存机制（热点股票缓存）

【数据采集】
- 使用yfinance批量抓取股票历史数据
- 支持断点续爬
- 支持批量导入数据库

【前端要求】
- 左右布局（侧边菜单 + 顶部导航）
- 股票列表页（表格 + 分页）
- 股票详情页（K线图）
- 使用Pinia管理状态
- 封装API请求模块（axios）

【性能优化】
- 后端分页 + 索引优化
- 前端懒加载
- 图表数据按需加载

【输出要求】
1. 后端完整项目结构 + 核心代码
2. 前端完整项目结构 + 核心代码
3. 数据库建表SQL
4. 关键模块代码（K线图、接口调用）
5. 可运行说明

请一步一步输出，每一部分代码都要完整可运行，不要省略。
```

---

# 🧠 二、后端专用 Prompt（更专业）

```text
你是高级Python后端工程师，请帮我设计一个高性能股票数据API服务，使用FastAPI实现。

要求：
- 支持查询股票10年历史数据
- 支持分页查询股票列表
- 使用SQLAlchemy操作MySQL
- 数据表结构包含：symbol, date, open, high, low, close, volume
- 使用Redis做缓存（热点股票）
- 提供接口：
  /stocks/history?symbol=AAPL&start=2015-01-01&end=2025-01-01
  /stocks/page?page=1&size=50
- 返回JSON结构规范统一（code, message, data）

额外要求：
- 分层架构（router/service/dao）
- 支持日志记录
- 支持异常处理
- 支持跨域

请输出完整项目代码结构 + 核心代码文件。
```

---

# 🎨 三、前端专用 Prompt（Vue3）

```text
你是资深前端架构师，请帮我用Vue3 + Vite + TypeScript + ant-design-vue生成一个股票管理后台系统。

要求：
- 左右布局（侧边栏 + 顶部导航）
- 使用Pinia做状态管理
- 使用Vue Router（history模式）
- 封装axios请求

页面包括：
1. 股票列表页（表格 + 分页 + 搜索）
2. 股票详情页（K线图）
3. 自选股页面

K线图要求：
- 使用ECharts
- 支持蜡烛图（candlestick）
- 支持成交量

UI主题：
primaryColor: #111827（深色风格）

请输出：
- 项目结构
- layout组件
- store代码
- API封装
- 页面代码
```

---

# 📊 四、K线图专用 Prompt（很关键）

```text
请帮我用ECharts实现一个股票K线图组件，基于Vue3 + TypeScript。

要求：
- 支持蜡烛图（open, close, low, high）
- 支持成交量柱状图
- 支持tooltip显示详细数据
- 支持缩放（dataZoom）
- 支持动态更新数据

输入数据格式：
[
  ["2024-01-01", open, close, low, high, volume]
]

请输出完整Vue组件代码（可直接使用）。
```

---

# ⚙️ 五、数据采集脚本 Prompt

```text
请帮我写一个Python脚本，用yfinance批量抓取美股10年历史数据并存入MySQL。

要求：
- 支持股票列表批量抓取
- 支持失败重试
- 支持断点续爬
- 每100只股票保存一次
- 避免被封（加入延迟）

数据库字段：
symbol, date, open, high, low, close, volume

请输出完整可运行脚本。
```

---

# 🧱 六、进阶（如果你要做商业级）

```text
在现有股票系统基础上，增加以下功能：

1. WebSocket实时行情推送
2. 技术指标（MA5, MA10, MACD）
3. 用户登录系统（JWT）
4. 自选股同步到后端
5. 数据可视化分析（ECharts）

要求：
- 后端FastAPI实现WebSocket
- 前端Vue3实时更新图表
- 代码完整可运行
```

---

# 💡 使用建议（非常关键）

👉 正确使用方式：

1️⃣ **不要一次全生成**
👉 按模块来（否则模型容易崩）

2️⃣ 推荐顺序：

```text
1. 后端
2. 数据采集
3. 前端
4. 图表
```

3️⃣ 如果生成质量不高，加一句：

```text
请以“生产级代码标准”输出，不要使用示例代码
```

---

# 🎯 如果你想再升级一层（我可以帮你）

我可以直接帮你做👇：

* ✅ 一套“股票系统商业架构设计图”
* ✅ ClickHouse百万级数据方案
* ✅ Docker一键部署
* ✅ 自动更新数据服务（cron + celery）

---

👉 直接说一句：

**「我要企业级版本」**
我会给你一套真正能上线赚钱的方案（不是练手项目）。

```



> 更新: 2026-04-21 16:07:29  
> 原文: <https://www.yuque.com/lixinsi/up90z7/eqqou4em42kc4u9w>