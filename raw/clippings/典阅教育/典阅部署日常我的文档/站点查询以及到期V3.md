# 站点查询以及到期V3

```plain
# 1. 临时设置脚本执行策略（解决脚本无法执行问题，执行完脚本后可根据需要改回原策略）
Set-ExecutionPolicy Bypass -Scope Process -Force

try {
    # 2. 导入 WebAdministration 模块，添加异常捕获确保模块加载反馈清晰
    if (-not (Get-Module -Name WebAdministration -ErrorAction SilentlyContinue)) {
        Import-Module WebAdministration -ErrorAction Stop
        Write-Host "已成功加载 WebAdministration 模块"
    }

    # 3. 定义连接字符串（请根据实际情况修改）  
    $connectionString = "Server=120.77.10.122;Database=bug;User Id=sa;Password=Dysoft0706!@#;"
    $connection = New-Object System.Data.SqlClient.SqlConnection($connectionString)
    $connection.Open()

    # 4. 输出表头，调整绑定信息显示格式
    Write-Host "站点名称`t状态`t绑定信息（IP:端口:域名）`t序列号`t过期日期`t许可证状态"
    Write-Host ("-" * 120)

    # 5. 准确获取所有 IIS 站点（兼容不同环境，优先用 Get-Website 补充 Get-WebSite ）
    $sites = @()
    if (Get-Command Get-WebSite -ErrorAction SilentlyContinue) {
        $sites = Get-WebSite
    } else {
        $sites = Get-Website
    }

    foreach ($site in $sites) {
        $siteName = $site.Name
        $siteState = $site.State.ToString()
        
        # 获取站点绑定信息（解析完整 IP:端口:域名）
        $bindingsArray = @()
        if ($site.Bindings) {
            foreach ($binding in $site.Bindings.Collection) {
                $bindingInfo = $binding.BindingInformation
                # 解析格式：IP:端口:域名（或 *:端口:域名、IP:端口 等）
                if ($bindingInfo -match '^(?<IP>\*|[\d\.]+):(?<Port>\d+)(:(?<Domain>.+))?$') {
                    $ip = $matches['IP']
                    $port = $matches['Port']
                    $domain = $matches['Domain']
                    
                    # 兼容低版本 PowerShell：使用 if-else 替代 ?? 运算符
                    if ([string]::IsNullOrEmpty($domain)) {
                        $domain = "无域名"
                    }
                    
                    $bindingsArray += "$ip:$port:$domain"
                } else {
                    $bindingsArray += "绑定格式异常: $bindingInfo"
                }
            }
        } else {
            $bindingsArray += "未配置绑定"
        }
        $bindings = $bindingsArray -join " | "

        # 6. 根据站点状态设置显示颜色
        $siteStateColor = switch ($siteState) {
            "Started" { "Green" }
            default { "Yellow" }
        }

        $sitePath = (Get-ItemProperty "IIS:\Sites\$siteName" -ErrorAction SilentlyContinue).PhysicalPath
        $webConfigPath = if ($sitePath) { Join-Path $sitePath "web.config" } else { "" }

        if (Test-Path $webConfigPath -PathType Leaf) {
            try {
                [xml]$config = Get-Content $webConfigPath -ErrorAction Stop
                $serialNoElement = $config.configuration.appSettings.add | 
                    Where-Object { $_.key -eq "SerialNo" } -ErrorAction SilentlyContinue

                if ($serialNoElement) {
                    $serialNoValue = $serialNoElement.value
                    $sqlQuery = "SELECT TOP 1 
                                CONVERT(varchar, endtime, 23) AS formattedEndtime,
                                (CASE WHEN endtime < GETDATE() THEN '已过期' ELSE '有效' END) AS licenseStatus
                             FROM dbo.tb_TimeMD5 
                             WHERE miyao = @SerialNo"
                    
                    $sqlCommand = $connection.CreateCommand()
                    $sqlCommand.CommandText = $sqlQuery
                    $sqlParam = $sqlCommand.Parameters.Add("@SerialNo", [System.Data.SqlDbType]::VarChar)
                    $sqlParam.Value = $serialNoValue

                    $reader = $sqlCommand.ExecuteReader()
                    if ($reader.Read()) {
                        $endtime = $reader["formattedEndtime"].ToString()
                        $licenseStatus = $reader["licenseStatus"].ToString()
                        
                        $licenseColor = if ($licenseStatus -eq "已过期") { "Red" } else { "Green" }

                        Write-Host "$siteName`t" -NoNewline
                        Write-Host "$siteState`t" -ForegroundColor $siteStateColor -NoNewline
                        Write-Host "$bindings`t" -NoNewline
                        Write-Host "$serialNoValue`t$endtime`t" -NoNewline
                        Write-Host "$licenseStatus" -ForegroundColor $licenseColor
                    } else {
                        Write-Host "$siteName`t" -NoNewline
                        Write-Host "$siteState`t" -ForegroundColor $siteStateColor -NoNewline
                        Write-Host "$bindings`t" -NoNewline
                        Write-Host "$serialNoValue`t未找到匹配记录" -ForegroundColor Yellow
                    }
                    $reader.Close()
                } else {
                    Write-Host "$siteName`t" -NoNewline
                    Write-Host "$siteState`t" -ForegroundColor $siteStateColor -NoNewline
                    Write-Host "$bindings`t" -NoNewline
                    Write-Host "未找到序列号配置" -ForegroundColor Gray
                }
            } catch {
                Write-Host "$siteName`t" -NoNewline
                Write-Host "$siteState`t" -ForegroundColor $siteStateColor -NoNewline
                Write-Host "$bindings`t" -NoNewline
                Write-Host "读取 web.config 失败: $_" -ForegroundColor Red
            }
        } else {
            Write-Host "$siteName`t" -NoNewline
            Write-Host "$siteState`t" -ForegroundColor $siteStateColor -NoNewline
            Write-Host "$bindings`t" -NoNewline
            Write-Host "未找到 web.config 文件" -ForegroundColor Gray
        }
    }
} catch {
    Write-Host "执行过程中发生错误: $_" -ForegroundColor Red
} finally {
    if ($connection -and $connection.State -eq "Open") {
        $connection.Close()
    }
    # 7. （可选）执行完脚本后恢复执行策略到原来的状态，若要保留 Bypass 可删除以下代码
    $originalPolicy = Get-ExecutionPolicy -Scope Process
    Set-ExecutionPolicy $originalPolicy -Scope Process -Force
}
```

```plain
# 1. 临时设置脚本执行策略（解决脚本无法执行问题，执行完脚本后可根据需要改回原策略）
Set-ExecutionPolicy Bypass -Scope Process -Force

try {
    # 2. 导入 WebAdministration 模块，添加异常捕获确保模块加载反馈清晰
    if (-not (Get-Module -Name WebAdministration -ErrorAction SilentlyContinue)) {
        Import-Module WebAdministration -ErrorAction Stop
        Write-Host "已成功加载 WebAdministration 模块"
    }

    # 3. 定义连接字符串（请根据实际情况修改）  
    $connectionString = "Server=120.77.10.122;Database=bug;User Id=sa;Password=Dysoft0706!@#;"
    $connection = New-Object System.Data.SqlClient.SqlConnection($connectionString)
    $connection.Open()

    # 4. 输出表头，调整绑定信息显示格式
    Write-Host "站点名称`t状态`t绑定信息（IP:端口:域名）`t序列号`t过期日期`t许可证状态"
    Write-Host ("-" * 120)

    # 5. 准确获取所有 IIS 站点（兼容不同环境，优先用 Get-Website 补充 Get-WebSite ）
    $sites = @()
    if (Get-Command Get-WebSite -ErrorAction SilentlyContinue) {
        $sites = Get-WebSite
    } else {
        $sites = Get-Website
    }

    foreach ($site in $sites) {
        $siteName = $site.Name
        $siteState = $site.State.ToString()
        
        # 获取站点绑定信息（解析完整 IP:端口:域名）
        $bindingsArray = @()
        if ($site.Bindings) {
            foreach ($binding in $site.Bindings.Collection) {
                $bindingInfo = $binding.BindingInformation
                # 解析格式：IP:端口:域名（或 *:端口:域名、IP:端口 等）
                if ($bindingInfo -match '^(?<IP>\*|[\d\.]+):(?<Port>\d+)(:(?<Domain>.+))?$') {
                    $ip = $matches['IP']
                    $port = $matches['Port']
                    $domain = $matches['Domain']
                    
                    # 替代 ?? 运算符：低版本兼容写法
                    if ([string]::IsNullOrEmpty($domain)) {
                        $domain = "无域名"
                    }
                    
                    # 修复变量语法：用字符串拼接替代直接嵌入
                    $bindingsArray += "$ip`:$port`:$domain"  # `: 转义避免语法冲突
                } else {
                    $bindingsArray += "绑定格式异常: $bindingInfo"
                }
            }
        } else {
            $bindingsArray += "未配置绑定"
        }
        $bindings = $bindingsArray -join " | "

        # 6. 根据站点状态设置显示颜色
        $siteStateColor = switch ($siteState) {
            "Started" { "Green" }
            default { "Yellow" }
        }

        $sitePath = (Get-ItemProperty "IIS:\Sites\$siteName" -ErrorAction SilentlyContinue).PhysicalPath
        $webConfigPath = if ($sitePath) { Join-Path $sitePath "web.config" } else { "" }

        if (Test-Path $webConfigPath -PathType Leaf) {
            try {
                [xml]$config = Get-Content $webConfigPath -ErrorAction Stop
                $serialNoElement = $config.configuration.appSettings.add | 
                    Where-Object { $_.key -eq "SerialNo" } -ErrorAction SilentlyContinue

                if ($serialNoElement) {
                    $serialNoValue = $serialNoElement.value
                    $sqlQuery = "SELECT TOP 1 
                                CONVERT(varchar, endtime, 23) AS formattedEndtime,
                                (CASE WHEN endtime < GETDATE() THEN '已过期' ELSE '有效' END) AS licenseStatus
                             FROM dbo.tb_TimeMD5 
                             WHERE miyao = @SerialNo"
                    
                    $sqlCommand = $connection.CreateCommand()
                    $sqlCommand.CommandText = $sqlQuery
                    $sqlParam = $sqlCommand.Parameters.Add("@SerialNo", [System.Data.SqlDbType]::VarChar)
                    $sqlParam.Value = $serialNoValue

                    $reader = $sqlCommand.ExecuteReader()
                    if ($reader.Read()) {
                        $endtime = $reader["formattedEndtime"].ToString()
                        $licenseStatus = $reader["licenseStatus"].ToString()
                        
                        $licenseColor = if ($licenseStatus -eq "已过期") { "Red" } else { "Green" }

                        Write-Host "$siteName`t" -NoNewline
                        Write-Host "$siteState`t" -ForegroundColor $siteStateColor -NoNewline
                        Write-Host "$bindings`t" -NoNewline
                        Write-Host "$serialNoValue`t$endtime`t" -NoNewline
                        Write-Host "$licenseStatus" -ForegroundColor $licenseColor
                    } else {
                        Write-Host "$siteName`t" -NoNewline
                        Write-Host "$siteState`t" -ForegroundColor $siteStateColor -NoNewline
                        Write-Host "$bindings`t" -NoNewline
                        Write-Host "$serialNoValue`t未找到匹配记录" -ForegroundColor Yellow
                    }
                    $reader.Close()
                } else {
                    Write-Host "$siteName`t" -NoNewline
                    Write-Host "$siteState`t" -ForegroundColor $siteStateColor -NoNewline
                    Write-Host "$bindings`t" -NoNewline
                    Write-Host "未找到序列号配置" -ForegroundColor Gray
                }
            } catch {
                Write-Host "$siteName`t" -NoNewline
                Write-Host "$siteState`t" -ForegroundColor $siteStateColor -NoNewline
                Write-Host "$bindings`t" -NoNewline
                Write-Host "读取 web.config 失败: $_" -ForegroundColor Red
            }
        } else {
            Write-Host "$siteName`t" -NoNewline
            Write-Host "$siteState`t" -ForegroundColor $siteStateColor -NoNewline
            Write-Host "$bindings`t" -NoNewline
            Write-Host "未找到 web.config 文件" -ForegroundColor Gray
        }
    }
} catch {
    Write-Host "执行过程中发生错误: $_" -ForegroundColor Red
} finally {
    if ($connection -and $connection.State -eq "Open") {
        $connection.Close()
    }
    # 7. （可选）执行完脚本后恢复执行策略到原来的状态，若要保留 Bypass 可删除以下代码
    $originalPolicy = Get-ExecutionPolicy -Scope Process
    Set-ExecutionPolicy $originalPolicy -Scope Process -Force
}
```

### execl 表格替换

1. <font style="color:rgba(0, 0, 0, 0.85) !important;">打开 Excel 的</font><font style="color:rgba(0, 0, 0, 0.85) !important;"> </font>**<font style="color:rgb(0, 0, 0) !important;">“查找和替换”</font>**<font style="color:rgba(0, 0, 0, 0.85) !important;"> </font><font style="color:rgba(0, 0, 0, 0.85) !important;">对话框（快捷键</font><font style="color:rgba(0, 0, 0, 0.85) !important;"> </font><code><font style="color:rgb(0, 0, 0);">Ctrl+H</font></code><font style="color:rgba(0, 0, 0, 0.85) !important;">）。</font>
2. <font style="color:rgba(0, 0, 0, 0.85) !important;">在</font><font style="color:rgba(0, 0, 0, 0.85) !important;"> </font>**<font style="color:rgb(0, 0, 0) !important;">“查找内容”</font>**<font style="color:rgba(0, 0, 0, 0.85) !important;"> </font><font style="color:rgba(0, 0, 0, 0.85) !important;">中输入</font><font style="color:rgba(0, 0, 0, 0.85) !important;"> </font><code><font style="color:rgb(0, 0, 0);">~*</font></code><font style="color:rgba(0, 0, 0, 0.85) !important;">（波浪线 + 星号）。</font>
3. <font style="color:rgba(0, 0, 0, 0.85) !important;">在</font><font style="color:rgba(0, 0, 0, 0.85) !important;"> </font>**<font style="color:rgb(0, 0, 0) !important;">“替换为”</font>**<font style="color:rgba(0, 0, 0, 0.85) !important;"> </font><font style="color:rgba(0, 0, 0, 0.85) !important;">中输入需要替换成的内容（例如</font><font style="color:rgba(0, 0, 0, 0.85) !important;"> </font><code><font style="color:rgb(0, 0, 0);">120.24.218.230</font></code><font style="color:rgba(0, 0, 0, 0.85) !important;">）。</font>
4. <font style="color:rgba(0, 0, 0, 0.85) !important;">点击 </font>**<font style="color:rgb(0, 0, 0) !important;">“替换”</font>**<font style="color:rgba(0, 0, 0, 0.85) !important;"> 或 </font>**<font style="color:rgb(0, 0, 0) !important;">“全部替换”</font>**<font style="color:rgba(0, 0, 0, 0.85) !important;">。</font>


> 更新: 2025-07-04 11:19:05  
> 原文: <https://www.yuque.com/lixinsi/hntyk2/svq6tdb9h5iocmhz>